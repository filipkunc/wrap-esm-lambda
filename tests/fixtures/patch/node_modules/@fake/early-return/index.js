// The early-exit CJS shape that defeats a plainly APPENDED tap: the CJS
// wrapper is a function, so the top-level `return` below means nothing after
// it ever runs — the tap must reach this module through the evaluation wrap
// (core/cjs-wrap.mts) instead. The module doubles as a probe for what that
// wrap must preserve: the ASI directive prologue (no semicolon), strict-mode
// semantics observed from inside, and Annex B function hoisting (`greet` is
// called above its declaration).
'use strict'
try {
  // eslint-disable-next-line no-undef
  undeclaredEarlyReturnProbe = 1
  exports.strict = false
} catch {
  exports.strict = true
}
exports.greet = greet
exports.hoisted = greet('hoist')
function greet(name) {
  return `hi:${name}`
}
if (exports.strict !== 'never') {
  return
}
exports.late = true
