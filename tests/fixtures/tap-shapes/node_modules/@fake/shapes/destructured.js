// Destructured const export: the names live inside a binding pattern. The
// rewrite demotes the whole pattern declaration so `greet` can be rebound.
export const { greet } = { greet: () => 'greet' }
