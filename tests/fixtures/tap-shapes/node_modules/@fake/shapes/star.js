// Bare star chain: star.js -> star-mid.js -> inner.js. `Inner` is invisible
// here statically; the transform walks the chain and appends a shadow
// export redirecting it through a rebindable local.
export * from './star-mid.js'
export const STAR_LOCAL = 'untouched'
