// Pure CJS in a plain `.js` file of a package with no `"type"` field —
// express's exact layout, and the shape only syntax detection classifies
// correctly at build time (no format hint, no telling extension or path).
'use strict'

function greet(name) {
  return 'hi:' + name
}

module.exports.greet = greet
