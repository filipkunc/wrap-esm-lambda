// The canonical Lambda shape: an exported const arrow function. A pure
// append cannot rebind this — the tap's rewrite path demotes it to `let`.
export const handler = async (event) => `hi:${event}`
