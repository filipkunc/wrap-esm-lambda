// Bare-SPECIFIER stars: the sources are packages, not relative files. The
// transform resolves them like an import would (exports map under the
// `import` condition; `"module"` before `"main"`) to walk their exports —
// the emitted shadow exports still import from the specifiers as written.
export * from '@fake/star-pkg'
export * from '@fake/star-plain'
