// The ESM tree — what the `import` condition of the exports map serves, and
// therefore what an `export * from "@fake/star-pkg"` forwards.
export class StarPkg {
  greet() {
    return 'star-pkg'
  }
}
