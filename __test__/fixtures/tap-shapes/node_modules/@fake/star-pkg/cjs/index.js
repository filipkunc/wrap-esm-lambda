// The CJS tree. If resolution picked this (wrong condition), the star walk
// would find no static exports and the tap would fail loudly — so a passing
// bare-star test proves the `import` condition won.
'use strict'
module.exports.StarPkg = class StarPkg {
  greet() {
    return 'star-pkg-cjs'
  }
}
