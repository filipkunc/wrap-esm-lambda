// ESM consumer of the pure-CJS package — the README quick-start corridor
// (express through a bundler) reduced to a deterministic fixture.
import { greet } from '@fake/cjs-lib'

console.log(greet('x'))
