// A cyclic star pair: legal ESM, and the shape that makes a memoizing walk
// tricky — a naive cache can store the truncated (incomplete) name set it
// computed while the cycle was unwinding.
export * from './cycle-b.js'
export const fromA = 'a'
