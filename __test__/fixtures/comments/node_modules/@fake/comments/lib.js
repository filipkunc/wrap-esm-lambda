/* license header: @fake/comments v1.0.0 */
// The exported const forces the tap's REWRITE path (const -> let demotion),
// so the whole module is regenerated (oxc) or edited (acorn) — the worst
// case for comment survival. The markers below are bundler-semantic:
// esbuild may drop the annotated call ONLY if the @__PURE__ comment
// survives the rewrite, and must keep the unannotated control either way.
export const handler = async (event) => `hi:${event}`

const _kept = globalThis.__welKeep && globalThis.__welKeep('KEPT_MARKER')
const _dropped = /* @__PURE__ */ (globalThis.__welPure || String)('PURE_DROPPED')

/*! KEEP-LEGAL @fake/comments */
